//! The `algraf` binary: argument parsing, command dispatch, and I/O.
//!
//! Supports `algraf render` and `algraf lsp` (see spec §0 and §22).

fn main() {
    // Command dispatch is implemented in a later task.
}
